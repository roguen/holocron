{
  "$schema": "https://raw.githubusercontent.com/microsoft/vcpkg-tool/main/docs/vcpkg.schema.json",
  "name": "holocron",
  "version-string": "0.8.8",
  "description": "GPU music visualization engine that is also the music player",
  "homepage": "https://github.com/roguen/holocron",
  "license": "GPL-3.0-or-later",
  "builtin-baseline": "39344dff01c5a5a0134caf2624cdd492f05d30ea",

  "$licence-note": "Holocron is GPL-3.0-or-later (D-010), so GPL dependencies are compatible. What is NOT acceptable is anything non-redistributable: --enable-nonfree FFmpeg stays excluded under any licence. libprojectM is ABSENT ON PURPOSE and must stay absent (D-012, D-039, issue 11): it is opened with LoadLibrary/dlopen at runtime and reached through its C API by function pointer, so it is neither linked nor shipped. Adding it here would turn a runtime option into a build dependency, break the 'runs with libprojectM absent' exit criterion, and put an LGPL library back inside the binary's link line for no benefit. See THIRD-PARTY-NOTICES.md.",
  "$ffmpeg-note": "default-features is FALSE deliberately, and the feature list is explicit. The port's 'gpl' and 'nonfree' features are not in its defaults today, but relying on that is relying on an upstream default we do not control - and 'nonfree' produces libraries that are unredistributable under ANY licence, which would be unfixable after the fact. Listing features also drops avdevice, avfilter and swscale, none of which an audio decoder uses.",
  "$ffmpeg-tls-note": "'openssl' is requested only where schannel is unavailable, which is what the '!windows' platform expression means (issue 239). FFmpeg is what opens the Plex stream and a media server is HTTPS, so FFmpeg needs a TLS backend; on Windows the port supplies schannel, which is part of the OS and is neither linked nor shipped. Asking for openssl EVERYWHERE would turn schannel off there - the portfile enables schannel only in the else branch of the openssl check - so the one platform that already worked would need re-testing and would gain a shipped TLS library and a second trust store to keep current, for nothing. Measured before the fix: CONFIG_HTTPS_PROTOCOL was 0 for arm64-android and 1 for x64-windows. Note '!windows' includes Linux, so the CI job builds OpenSSL too; that is a one-time cost against the vcpkg binary cache, and the narrower 'android' expression was rejected because it would silently re-arm the same bug for any future non-Windows target. OpenSSL 3.x is Apache-2.0, one-way compatible with GPL-3.0; the feature also pulls in ffmpeg's 'version3', so FFmpeg is LGPL-3.0-or-later there rather than 2.1. Both are recorded in THIRD-PARTY-NOTICES.md.",
  "$dependency-notes": {
    "glm": "MIT. Required by track_context.hpp.",
    "pocketfft": "BSD-3-Clause, header-only. FFT for the analysis stage (#9). The implementation SciPy uses. FFTW was available under D-010 but rejected: a 2048-point real FFT at 93.75 Hz does not justify the weight.",
    "libebur128": "MIT. ITU-R BS.1770-4 loudness for AudioFrame::loudness_short (#9, D-024). Hand-rolling the K-weighting filter and gating would produce a number that looks plausible and is not comparable across tracks, which would destroy the only reason D-007 leaves the field un-normalized.",
    "catch2": "BSL-1.0, GPL-compatible. Test-only, hence the feature.",
    "tomlplusplus": "MIT, header-only. Crystal manifests (M2), and gatekeeper.toml when a loader exists. TOML rather than JSON because these files are edited by hand by crystal authors: comments survive, trailing commas are not an error, and a typo in a key is a parse error at a named line rather than a silently-ignored member.",
    "glad": "MIT. The GL function loader for the 4.5 core context (D-012). Issue #13 proposed committing glad2's generated output under third_party/ to avoid version drift; the manifest gets the same guarantee -- glad is pinned by the same builtin-baseline as everything else and moves only when the baseline does -- without carrying generated code in the tree. Features are explicit: 'loader' and 'gl-api-45', so nothing above 4.5 is even declared and a 4.6-only call is a compile error rather than a runtime surprise on a lesser GPU.",
    "cpp-httplib": "MIT, header-only. The Plex Companion HTTP surface (M5). default-features is FALSE deliberately: the port's optional features are OpenSSL, zlib and brotli, and none of them applies here -- Companion is plain HTTP on the LAN, and pulling OpenSSL in for a server that will never terminate TLS would add a large dependency and its licence exception handling for nothing. Hand-rolling was considered and rejected: the endpoints themselves are simple GETs, but HTTP request parsing is not, and this is a protocol with no specification to check an answer against, so a parsing bug would present as 'the device does not appear' with no way to tell it apart from a protocol mistake.",
    "sdl3": "Zlib AND MIT AND Apache-2.0, GPL-compatible. The first sink backend (#1) and, later, the window and GL 4.5 context. default-features is FALSE, which costs nothing on the target: every one of the port's default features (ibus, wayland, x11) is platform-gated to Linux, so Windows resolves to the same set either way. Turning them off spares the Linux CI job a chain of system dev packages for a platform that is a second compiler, not a deployment target (D-022). SDL's built-in dummy audio driver needs no feature and no hardware, which is how the sink is exercised headless on both platforms."
  },

  "dependencies": [
    {
      "name": "ffmpeg",
      "default-features": false,
      "features": [
        "avcodec",
        "avformat",
        "swresample",
        {
          "name": "openssl",
          "platform": "!windows"
        }
      ]
    },
    {
      "name": "cpp-httplib",
      "default-features": false
    },
    "glm",
    "libebur128",
    "pocketfft",
    {
      "name": "sdl3",
      "default-features": false
    },
    {
      "name": "glad",
      "default-features": false,
      "features": [
        "loader",
        "gl-api-45"
      ],
      "platform": "!android"
    },
    "tomlplusplus"
  ],

  "features": {
    "tests": {
      "description": "Build the test suite",
      "dependencies": [
        "catch2"
      ]
    }
  },

  "default-features": [
    "tests"
  ]
}
